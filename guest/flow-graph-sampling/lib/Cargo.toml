[package]
name = "flow-graph-sampling-guest-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
alloy-sol-types = { workspace = true }
serde = { version = "1", features = ["derive"] }
walrus_core = { path = "../../../walrus-core" }
