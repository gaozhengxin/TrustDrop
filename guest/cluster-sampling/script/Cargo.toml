[package]
name = "cluster-sampling-script"
version = "0.1.0"
edition = "2021"
autobins = false

[[bin]]
name = "cluster-sampling-client"
path = "src/main.rs"

[features]
execute = []
network = []

[dependencies]
alloy-sol-types = { workspace = true }
bincode = "1.3"
drop-lib = { path = "../../../drop-lib" }
cluster-sampling-guest-lib = { path = "../lib" }
hex = "0.4.3"
serde_json = "1"
sp1-sdk = { version = "=6.2.4", features = ["network"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
trustdrop-cluster-sampling = { path = "../../../extensions/dataset-sampling/cluster/lib" }
walrus_core = { path = "../../../walrus-core" }

[build-dependencies]
sp1-build = "=6.2.4"
